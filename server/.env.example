# Server Configuration
NODE_ENV=development
PORT=4000

# CORS (use http://localhost:3000 if running Vite dev proxy separately)
CORS_ORIGIN=http://localhost:4000

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=signage
DB_PASSWORD=signage
DB_NAME=signage

# Session
SESSION_SECRET=

# WebAuthn
RP_ID=localhost
ORIGIN=http://localhost:4000
